# credentials and local configuration -- commit the .example files instead
secrets.env
correlator.env
# run state: proposals, jobs, undo logs, operator overrides, correlation dumps (tenant data)
state/
correlations/
/*.json
*.conf
.env
*.jsonl
*.log
# python / editor / backups
__pycache__/
*.pyc
.DS_Store
*.bak*
