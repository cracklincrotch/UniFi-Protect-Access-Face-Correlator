# Copy to secrets.env and chmod 600. Environment variables of the same name override.
PROTECT_HOST=unvr.pc
PROTECT_API_KEY=your-protect-integration-api-key
PROTECT_USER=protect-admin-user
PROTECT_PASS=protect-admin-password
PUSHOVER_TOKEN=your-pushover-application-token
PUSHOVER_USER=your-pushover-user-key
