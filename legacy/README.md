Earlier one-off tools, kept for reference. None of these is wired to anything:
`scan.py` read TSV dumps that no longer exist, `apply.py` expected a
`PROTECT_API_KEY` line in an NVR config that does not carry one, and
`face_match_nightly.py` was folded into `face_auto_label.py`.
